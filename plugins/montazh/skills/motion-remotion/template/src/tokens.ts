// Палитра по умолчанию. Правило важнее значений: у каждого цвета ОДНА роль.
// Нет роли — нет цвета. Свои цвета подставляйте прямо здесь или темой в themes.ts.
export const T = {
  ink: "#17160F",       // почти чёрный: фон захватов, полосы, плотные зоны
  inkDeep: "#0E0E0E",   // сцена под видео
  paper: "#FBFAF6",     // тёплая бумага: фон карточек
  paper2: "#FFFFFF",    // поверхность внутри карточки (скриншот-пруф)
  lime: "#C6F24E",      // ДЕЙСТВИЕ / выигрыш / активное состояние
  limeWash: "#EAF7B8",  // заливка-подсветка под лаймом
  violet: "#6D5AE6",    // мета / пояснение / CTA-слой
  muted: "#6C6B61",     // пояснения, никогда не спорит с заголовком
  line: "#E7E5DB",      // тонкие границы вместо тяжёлых обводок
  red: "#C8453B",       // ТОЛЬКО статус-риск: цена ошибки, «было»
  white: "#FFFFFF",
} as const;

export const R = { section: 28, card: 18, control: 10 } as const;
